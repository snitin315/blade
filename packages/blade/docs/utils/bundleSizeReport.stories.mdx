import { Meta } from '@storybook/addon-docs';
import { Text } from '~components/Typography';
import { Table } from '~utils/storybook/PopoverVsTooltip';

<Meta title="Utils/Bundle Size" />

# Bundle Size Report

export const BundleSizeReport = () => {
  const tableData= BUNDLE_SIZE_DATA;

  return (
    <Table >
      <thead>
        <tr>
          <th align="left">
            Component
          </th>
          <th align="left">
            Size
          </th>
        </tr>
      </thead>
      <tbody>
        {tableData.map((tableItem, index) => (
          <tr key={index} item={tableItem}>
            <td key={index}> <Text weight="bold"> {tableItem.name} </Text></td>
            <td key={index}>{`${tableItem.size/1000} kB`}</td>
          </tr>
        ))}
      </tbody>    
    </Table>
  );
};

<BundleSizeReport />
